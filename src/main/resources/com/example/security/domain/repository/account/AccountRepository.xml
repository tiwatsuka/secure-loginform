<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.security.domain.repository.account.AccountRepository">

    <resultMap id="accountResultMap" type="Account">
        <id property="username" column="username" />
        <result property="password" column="password" />
        <result property="firstName" column="first_name" />
        <result property="lastName" column="last_name" />
        <result property="role" column="role" />
        <result property="lastPasswordChangeDate" column="last_password_change_date" typeHandler="DateTimeTypeHandler"/>
        <result property="unlockDate" column="unlock_date" typeHandler="DateTimeTypeHandler"/>
    </resultMap>

    <select id="findOne" parameterType="string" resultMap="accountResultMap">
        SELECT
            username,
            password,
            first_name,
            last_name,
            role,
            last_password_change_date,
            unlock_date
        FROM
            account
        WHERE
            username = #{username}
    </select>
    
    <update id="updatePassword" parameterType="Account">
    	UPDATE
    		account
    	SET
    		password = #{password},
    		last_password_change_date = #{lastPasswordChangeDate,typeHandler=DateTimeTypeHandler}
    	WHERE
    		username = #{username}
    </update>
    
    <update id="updateUnlockDate" parameterType="Account">
    	UPDATE
    		account
    	SET
    		unlock_date = #{unlockDate,typeHandler=DateTimeTypeHandler}
    	WHERE
    		username = #{username}
    </update>
</mapper>

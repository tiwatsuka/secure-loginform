<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.security.domain.repository.account.AccountAuthenticationLogRepository">

    <resultMap id="accountAuthenticationLogResultMap" type="AccountAuthenticationLog">
        <id property="username" column="username" />
        <id property="authenticationTimestamp" column="authentication_timestamp" typeHandler="DateTimeTypeHandler"/>
        <result property="isSuccess" column="success" />
    </resultMap>

    <select id="findBySuccess" resultMap="accountAuthenticationLogResultMap">
    <![CDATA[
        SELECT
            username,
            authentication_timestamp,
            success
        FROM
            account_authentication_log
        WHERE
            username = #{username} AND
            success = #{isSuccess}
        ORDER BY authentication_timestamp DESC
        LIMIT #{nbLog}
    ]]>
    </select>
    
    <select id="findByDulation" resultMap="accountAuthenticationLogResultMap">
    <![CDATA[
        SELECT
            username,
            authentication_timestamp,
            success
        FROM
            account_authentication_log
        WHERE
            username = #{username} AND
            authentication_timestamp BETWEEN #{from,typeHandler=DateTimeTypeHandler} AND #{to,typeHandler=DateTimeTypeHandler}
        ORDER BY authentication_timestamp DESC
    ]]>
    </select>
    
    <insert id="insert" parameterType="AccountAuthenticationLog">
    <![CDATA[
        INSERT INTO account_authentication_log (
            username,
            authentication_timestamp,
            success
        ) VALUES (
	        #{username},
            #{authenticationTimestamp, typeHandler=DateTimeTypeHandler},
            #{isSuccess}
        )
    ]]>
    </insert>
    
    <select id="findLatestLogs" resultMap="accountAuthenticationLogResultMap">
    <![CDATA[
        SELECT
            username,
            authentication_timestamp,
            success
        FROM
            account_authentication_log
        WHERE
            username = #{username}
        ORDER BY authentication_timestamp DESC
        LIMIT #{nbLog}
    ]]>
    </select>
</mapper>
